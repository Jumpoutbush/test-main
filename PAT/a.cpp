#include <iostream>
using namespace std;
class Sort{
public:
    bool cmp(int x, int y){
        if(x != y)
            return x < y;
    }
    void mysort(int& a){
        if(a)
    }
    int a[100];
};
int main()
{
    Sort s = Sort();
    for(int i = 0; i < 5; i++)
    {
        scanf("%d", &s.a[i]);
    }
}