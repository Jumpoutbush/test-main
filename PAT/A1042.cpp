#include<bits/stdc++.h>
using namespace std;
const int N = 54;
int start[N+1], end[N+1], next[N+1];
char mp[5] = {'S', 'H', 'C', 'D', 'J'};
int main()
{
    int K;
    scanf("%d", &K);
    for(int i = 1; i <= N; i++)
    {
        start[i] = i;
    }

}